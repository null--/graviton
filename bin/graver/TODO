[ HIGH PRIORITY PROJECT ]

standard config file (maybe xml format)
	- define compiler flags
	- define link flags
	- define dependencies
	
standard project file
	- pre build script (optional - user-defined)
	- build (standard + user-defined)
	
compilers:
	gcc, mingw, clang, cl
	
out archs:
	linux, osx, win, android
